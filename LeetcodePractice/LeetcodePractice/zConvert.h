#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class zConvert
{
public:
	string solution(string s, int numRows);
	void verification();
	string practice(string s, int numRows);
};

