#pragma once
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class majorityElement
{
public:
	int solution(vector<int>& nums);
	void verification();
	int easySolution(vector<int>& nums);
};

