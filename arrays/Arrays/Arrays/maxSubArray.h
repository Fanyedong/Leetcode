#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class maxSubArray
{
public:
	int solution(vector<int>& nums);
	void verification();
	int fastSolution(vector<int>& nums);
};

