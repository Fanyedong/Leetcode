#pragma once
#include <iostream>
#include <vector>
using namespace std;

class singleNumber
{
public:
	int solution(vector<int>& nums);
	void verification();
	int easySolution(vector<int>& nums);
};

