#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

class maxProfit
{
public:
	int solution(vector<int>& prices);
	void verification();
	int fastSolution(vector<int>& prices);
};

